[project]
name = "gestor-cli"
version = "0.1.0"
description = "Gestor Cli"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.0",
    "rich>=14.2.0",
]

[project.scripts]
gestor = "gestor_cli.main:main"
